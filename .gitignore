# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/

# EXT-TO-JSON project specifics
# Decompilation toolchain (large binaries — downloaded by START.bat / scripts/download-tools.ts)
/tools/
/downloads/
# Conversion work directories (temp)
/work/
# Uploaded APKs (user data)
/upload/
# Test apks
/test-apks/
# SQLite database (regenerated from schema; canonical data is in /converted)
/db/*.db
/db/*.db-journal

# Sandbox infra (not part of the user-facing project)
/.zscripts/
Caddyfile
# Sandbox test infra
/tests/
